Run each PUT command from the directory containing the exact asset file. Then submit the Design
System with the returned s3Key, byteSize, and sha256 metadata. Do not send asset bytes through the
application API.
