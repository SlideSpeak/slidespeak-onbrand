Get the Design System, including Brand Kit and Presentation Kit. Brand Kit visual assets include
assetHandle, filename, and mimeType metadata. To use Logo or Decorative Asset files, call
materialize_brand_kit_assets, then run the returned shell commands to download exact files into your
workspace.
