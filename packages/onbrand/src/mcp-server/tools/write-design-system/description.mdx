Create or replace a complete Design System from source material the user provides (URL, PDF, PPTX,
image, existing deck, notes, or any combination). Before calling: inspect the source, extract the
organization's visual language, colors, logo/decorative asset files, slide canvas, and write a
detailed slide Design Prompt. Upload exact asset files first with
prepare_design_system_asset_uploads, then pass the returned s3Key/byteSize/sha256 references here.
Do not send file bytes through MCP and do not include invented or approximate logos.
